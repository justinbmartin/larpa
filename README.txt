Larpa

First Time Setup

python -m venv .venv
pip install -r requirements.txt
source ./venv/bin/activate

Will prepend (.venv) to your console header.

Only sourcing the virtual environment is required each time you want to run this python script.

Ctrl+C to exit